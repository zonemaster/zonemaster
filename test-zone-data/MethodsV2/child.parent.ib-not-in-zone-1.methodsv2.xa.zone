$ORIGIN child.parent.ib-not-in-zone-1.methodsv2.xa.
$TTL 3600

@       SOA     ns1 admin. (
                2024053001      ; serial
                6h              ; refresh
                1h              ; retry
                1w              ; expire
                1d )            ; minimum

		NS	ns1
		NS	ns2

; No address records for IB NS.

; EOF

